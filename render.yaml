services:
  - type: web
    name: event-management-system
    env: python
    buildCommand: ""
    startCommand: gunicorn backend.app:app
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
